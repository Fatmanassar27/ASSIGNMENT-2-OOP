namespace ASSIGNMENT_2_OOP.Encapsolation
{
    internal struct PhoneBook
    {
        //int size;
        //string[] Name;
        //int[] PhoneNumbers;

        ////setter function of size 
        //public int Size
        //{
        //    get { return size; }
        //}

        //// constructor
        //public PhoneBook(int size)
        //{
        //    this.size = size;
        //    Name = new string[size];
        //    PhoneNumbers = new int[size];
        //}

        //// Add Person function
        //public void addperson(int position, string name, int number)
        //{
        //    if (name != null && PhoneNumbers is not null)
        //    {
        //        if (position < size)
        //            PhoneNumbers[position] = number;
        //        Name[position] = name;
        //    }
            //}

            //// Encapsulation 
            //public int GetPhonenumber(string name)
            //{
            //    if (PhoneNumbers is not null && Name is not null)
            //    {
            //        for (int i = 0; i < Name.Length; i++)
            //        {
            //            if (Name[i] == name)
            //            {
            //                return PhoneNumbers[i];
            //            }
            //        }
            //    }
            //    return -1;
            //}

            ////setter
            //public void add_newnumber(string name, int new_number)
            //{
            //    if (name is not null && PhoneNumbers is not null)
            //    {
            //        for (int i = 0; i < Name.Length; i++)
            //        {
            //            if (Name[i] == name)
            //            {
            //                PhoneNumbers[i] = new_number;
            //            }

            //        }
            //    }
            //}

            ////indexer 
            //public int this[string name]
            //{
            //    get
            //    {
            //        if (Name is not null && PhoneNumbers is not null)
            //            for (int i = 0; i < Name.Length; i++)
            //                if (Name[i] == name)
            //                {
            //                    return PhoneNumbers[i];
            //                }
            //        return -1;
            //    }

            //    set
            //    {
            //        if (Name is not null && PhoneNumbers is not null)
            //            for (int i = 0; i < Name.Length; i++)
            //                if (Name[i] == name)
            //                {
            //                    PhoneNumbers[i] = value;
            //                }
            //    }
            //}

            //// indexer overloading 
            //public string this [int index]
            //    {
            //       get
            //       {
            //        return $"{index} : {Name[index]} : {PhoneNumbers[index]}";
            //       }
            //    }


        }
}
